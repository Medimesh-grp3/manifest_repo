# ambulance Service - Active (Blue)
apiVersion: v1
kind: Service
metadata:
  name: medimesh-ambulance-svc
  namespace: {{ .Values.namespaces.backend }}
  labels:
    app: {{ .Values.name }}
    tier: backend
spec:
  selector:
    app: {{ .Values.name }}
    tier: backend
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
---
# ambulance Service - Preview (Green)
apiVersion: v1
kind: Service
metadata:
  name: medimesh-ambulance-svc-preview
  namespace: {{ .Values.namespaces.backend }}
  labels:
    app: {{ .Values.name }}
    tier: backend
spec:
  selector:
    app: {{ .Values.name }}
    tier: backend
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
